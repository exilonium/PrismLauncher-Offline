# Publish workflow is disabled for this fork.
# The upstream Winget job targets PrismLauncher.PrismLauncher which is not
# registered for this fork. Re-enable and update once a separate Winget
# package is registered.
name: Publish

on:
  workflow_dispatch: # manual trigger only; not auto-triggered on release

permissions: {}

jobs:
  placeholder:
    name: Disabled (fork - no Winget package)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Publish workflow is disabled for this fork. See publish.yml for details."
